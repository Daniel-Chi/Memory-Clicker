import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//create and render the virtual DOM in the div with id "root"
ReactDOM.render(<App />, document.getElementById('root'));